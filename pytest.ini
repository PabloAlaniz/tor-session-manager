[pytest]
# Disable autoloading our own pytest11 plugin during this project's own test
# run: it would import the whole package at startup (before coverage begins),
# deflating the coverage report. The plugin is exercised directly in
# tests/test_pytest_plugin.py instead. Consumer projects still autoload it.
addopts = -q -p no:tor_session_manager
